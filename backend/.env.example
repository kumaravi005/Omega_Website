# Copy this file to `.env` in this folder for local development.
# Never commit the real .env file — see the root .gitignore.

# Port the API server listens on.
PORT=4000

# The frontend origin allowed to call this API (CORS). Use the site's real
# production URL once it is deployed.
FRONTEND_ORIGIN=http://localhost:5173
