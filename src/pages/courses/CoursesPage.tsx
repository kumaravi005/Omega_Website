import { PagePlaceholder } from '@/components/common/PagePlaceholder'

export default function CoursesPage() {
  return (
    <PagePlaceholder
      title="Courses"
      plannedContent="An overview of all courses offered, linking to each course page."
    />
  )
}
