import { PagePlaceholder } from '@/components/common/PagePlaceholder'

export default function ContactPage() {
  return (
    <PagePlaceholder
      title="Contact"
      plannedContent="Centre address, phone numbers, timings, map and an enquiry route."
    />
  )
}
