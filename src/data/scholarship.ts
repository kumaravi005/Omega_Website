import type { ScholarshipInfo } from '@/types/content'

/** Scholarship / O-SAT details. Fill in only once the institute confirms the scheme. */
export const scholarship: ScholarshipInfo = {
  slabs: [],
}
