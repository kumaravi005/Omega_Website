import { SectionPlaceholder } from '@/components/common/SectionPlaceholder'

/** Planned: course cards from data/courses.ts linking to /courses/:slug. */
export function CoursesSection() {
  return (
    <SectionPlaceholder
      id="courses"
      title="Courses"
      plannedContent="Foundation, JEE and NEET programmes, driven by the courses data."
    />
  )
}
