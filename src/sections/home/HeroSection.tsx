import { SectionPlaceholder } from '@/components/common/SectionPlaceholder'

/** Planned: headline, supporting line, primary CTAs, hero image (src/assets/hero). */
export function HeroSection() {
  return (
    <SectionPlaceholder
      id="hero"
      title="Hero"
      plannedContent="Main headline, short institute positioning and admission / enquiry calls to action."
    />
  )
}
