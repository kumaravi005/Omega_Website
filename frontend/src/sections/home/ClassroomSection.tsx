import { SectionPlaceholder } from '@/components/common/SectionPlaceholder'

/** Planned: photo gallery from data/gallery.ts (src/assets/classroom). */
export function ClassroomSection() {
  return (
    <SectionPlaceholder
      id="classroom"
      title="The offline classroom experience"
      plannedContent="Photos and highlights of in-person classes and the campus, driven by the gallery data."
    />
  )
}
