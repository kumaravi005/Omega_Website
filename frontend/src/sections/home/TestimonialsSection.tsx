import { SectionPlaceholder } from '@/components/common/SectionPlaceholder'

/** Planned: testimonial carousel/grid from data/testimonials.ts. */
export function TestimonialsSection() {
  return (
    <SectionPlaceholder
      id="testimonials"
      title="Student and parent voices"
      plannedContent="Testimonials shared with permission, driven by the testimonials data."
    />
  )
}
