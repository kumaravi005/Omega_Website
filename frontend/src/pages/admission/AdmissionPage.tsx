import { PagePlaceholder } from '@/components/common/PagePlaceholder'

export default function AdmissionPage() {
  return (
    <PagePlaceholder
      title="Admission"
      plannedContent="The admission process, required documents and an enquiry route for offline batches."
    />
  )
}
