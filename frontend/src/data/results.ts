import type { ResultEntry } from '@/types/content'

/** Verified student results. Add only results the institute can document. Photos go in src/assets/students. */
export const results: ResultEntry[] = []
